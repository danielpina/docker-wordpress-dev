# docker-wordpress-dev
A docker compose file for developing Wordpress themes and plugins

Run docker-compose up -d to create the containers
Site accessible via http://localhost:8080

ToDo: create database dump on docker-compose stop and make sure the same database is loaded on docker-composer start
